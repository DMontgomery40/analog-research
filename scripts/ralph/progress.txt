# Ralph Progress Log
Started: 2026-02-13
---
## Codebase Patterns
- This is a static HTML site on Netlify — no build step, no framework, no npm
- All CSS is inline in index.html <style> block
- Dark mode via prefers-color-scheme media query with CSS custom properties
- Netlify Forms with data-netlify="true" and honeypot spam protection
- netlify-forms.html exists as a standalone form detection file for Netlify's build bot
- David's LinkedIn: https://www.linkedin.com/in/david-montgomery-5a07656b/
- David's GitHub: https://github.com/DMontgomery40
- These should be embedded tastefully in footer and JSON-LD, not plastered everywhere
---

## 2026-02-13 - US-001
- Implemented semantic HTML and structured data foundation
- Changed h1 to include primary keyword "agentic research"
- Added `<nav>` element in header with aria-label
- Added `aria-hidden="true"` to decorative logo icon
- Added `aria-labelledby` with matching IDs to all 7 sections
- Converted use case divs to `<article>` elements
- Added JSON-LD Organization block (name, url, description, founder with sameAs, foundingDate, nonprofitStatus 501(c)(3), additionalType NonprofitType)
- Added JSON-LD WebSite block (name, url, description)
- Both JSON-LD blocks validated as valid JSON
- Files changed: index.html
- **Learnings for future iterations:**
  - `replace_all` is dangerous with partial class name matches (e.g. "case" matched "cases" and "case-icon") — use more specific strings or do targeted replacements
  - The page already had `lang="en"`, semantic `<header>`, `<main>`, `<section>`, `<footer>`, and no `<img>` tags needing alt attributes
  - CSS selectors like `.case` still work fine with `<article class="case">` — element type doesn't matter for class selectors
---

## 2026-02-13 - US-002
- Implemented meta tags, Open Graph, and Twitter Cards
- Updated meta description to exactly 150 characters (was 87, now within 150-160 range)
- Added meta keywords with 12 relevant terms (required 10+)
- Added meta robots "index, follow"
- Added canonical link to https://analog-research.org
- Added og:site_name "AnalogResearch"
- Added og:image pointing to /og-image.png (will be created in US-010)
- Added og:image:width (1200) and og:image:height (630)
- Added twitter:card "summary_large_image"
- Added twitter:title, twitter:description, twitter:image
- Files changed: index.html
- **Learnings for future iterations:**
  - og:image references /og-image.png which doesn't exist yet — US-010 will create it
  - Meta description at exactly 150 chars is at the low end of the range but valid
  - og:image:width and og:image:height are important for social platforms to render previews without re-fetching
---
